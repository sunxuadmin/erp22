# REQ-013 业务页面表头与表格配置覆盖补充

## 状态

- 需求：`REQ-013`
- 当前状态：`ARCHIVED / DOC_COMPLETE / IMPLEMENTED_BY_FOLLOW_UP_TASK`
- Git提交：未授权
- 数据库执行：未授权
- 部署：未授权

## 目标

把当前导航中的 8 个业务页面纳入同一份功能覆盖说明，明确现有 5 个页面的统一表头/表格配置能力，以及评审分配、评分汇总、签名表汇总 3 个页面与现有能力的差异，为后续逐页注册和运行时配置实现提供受控边界。

## 范围

- 涉及模块：产品功能覆盖矩阵、REQ-013 活动任务索引；
- 预计文件：本任务卡、`CREHN_DYZ_FUNCTION_COVERAGE.md`、`TASKS.md`；
- 数据/API/配置：仅增加文档覆盖登记；不新增运行时 `page_key`、`table_key`、权限、接口、数据库字段或配置数据。

## 禁止范围

- 不修改 `backend/`、`frontend/`、`portal/` 或 `deploy/`；
- 不提前确定 3 个新增页面的运行时稳定键，待后续页面注册任务依据路由、菜单和后端权限统一定名；
- 不改变评分、签名、状态机、数据范围、导出敏感级别或审计语义；
- 不执行 SQL、不连接服务器、不构建、不部署、不提交或推送 Git。

## 验收

- [x] 功能覆盖矩阵对比列出 5 个现有统一配置页面和 3 个待接入页面；
- [x] 评审分配的分配/编辑/停用操作、评分汇总的只读约束、签名表汇总的快照与审计约束已写明；
- [x] 记录表头、筛选、状态、表格列、操作文案、显示/导出分离和必要列保护边界；
- [x] 记录组件或操作缺少权限时提示“请先在角色权限管理中增加权限”，且配置不得自动授权；
- [ ] 运行时页面注册、统一配置接口、浏览器验收和服务端权限验证：后续任务；

## 实际变更与证据

### 修改文件与原因

- `docs/CREHN-DOCS-V1.0-MASTER/01-产品设计/CREHN_DYZ_FUNCTION_COVERAGE.md`：新增 8 个业务页面的表头/表格配置覆盖明细及边界；
- `TASKS.md`：登记本活动任务；
- 本任务卡：记录本次文档范围、未实施的代码边界和后续垂直切片。

### 检查结果

- `VERIFIED`：新增表格为有效 Markdown 表格；
- `VERIFIED`：仅修改文档和任务索引，未修改代码、SQL、部署资产；
- `NOT_RUN`：未执行构建、SQL、服务器、浏览器和部署验证。

### 后续实施边界

后续应按“页面注册与稳定键 → 表头/表格配置版本 → 组件兼容与权限缺口 → 页面迁移 → 角色浏览器验收”拆分任务。新增页面不得因配置显示而自动获得权限；页面必要识别列、评分/签名审计字段和服务端状态规则必须继续受保护。

### 授权状态

- 文档修改：本轮已授权并完成；
- 代码、SQL、服务器、构建、部署：未授权、未执行；
- Git提交和推送：未授权、未执行。
