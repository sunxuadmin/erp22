# 2026-08-01 TEST部署健康检查等待修复

## 状态

- 需求：`REQ-010`
- 当前状态：`ARCHIVED / VERIFIED_THROUGH_0.1.9`
- 代码/部署资产修改：已获授权
- 构建、资产暂存、TEST部署：已获授权
- Git提交与推送：未授权；本任务不推送
- 数据库初始化：禁止

## 目标

让正式TEST部署在容器已启动但Web健康检查仍处于`starting`时进行受控等待，避免将正常启动时序误判为部署失败并触发回滚。

## 范围

- 修改：`deploy/linux/crehn-deploy.sh`的服务健康核验等待逻辑；
- 新建：本任务卡并更新`TASKS.md`活动索引；
- 后续：以新的不可变TEST版本重新构建、暂存、部署和浏览器验收。

## 禁止范围

- 不执行数据库初始化或迁移；
- 不删除或覆盖既有不可变归档；
- 不修改项目A；
- 不连接ECS 2；
- 不推送Git或发布标签。

## 验收

- [ ] Bash语法和静态差异检查通过；
- [ ] Web/Backend健康检查等待在上限内结束，失败仍保留明确回滚；
- [ ] `0.1.4-test`三张应用镜像和归档修订一致；
- [ ] 完整TEST服务健康，MinIO-init完成；
- [ ] 项目A五容器基线一致；
- [ ] 登录态、角色权限、工作台配置保存回读和无权限提示浏览器验收通过。

## 实际变更与证据

待实施后回填。
