# 2026-08-01 Git历史与任务状态收口

## 状态

- 需求：`REQ-008`、`REQ-009`、`REQ-010`、`REQ-013`
- 当前状态：`ARCHIVED / VERIFIED / HISTORY_REWRITTEN / PUSH_NOT_AUTHORIZED`
- 文档状态核对、`backup_sync.ps1`安全门禁和本地历史改写：已授权并完成实施
- 8张已完成任务卡归档及本地文档提交：已授权并完成
- Git推送：未授权、未执行

## 目标

在继续`0.1.2-test`构建前，使需求索引、活动任务状态和实际Git历史一致，拆分混合提交，阻断未经明确确认的自动推送，并将旧构建和数据库失败链统一交给新的恢复任务承接。

## 改写前证据与恢复点

- 改写前HEAD为`4d400b4`，工作区干净；
- 完整恢复bundle已保存为`.git/codex-backups/pre-rewrite-4d400b4.bundle`并通过`git bundle verify`；
- Synology远端只读确认`origin/main=c785ea1`；
- GitHub远端已改为当前仓库局部HTTPS地址，通过Git Credential Manager只读确认`github/main=7a2f8ce`；
- 两个远端分支均为改写后`main`的祖先，未对远端执行写操作。

## 已执行的Git整理

共享历史保持到`7a2f8ce`不变：

1. 后端`3246b45`和前端`3f7f546`保持原提交及哈希；
2. `11b6cf8`统一承载V005脚本、后端SQL清单、数据库Dockerfile和部署迁移清单；
3. `8b1b409`独立承载Git同步脚本，并增加默认只读、显式远端、`-Apply`、精确确认串、`main:main`和标签开关；
4. 工作台文档、数据库初始化修复、全页面配置规范和LF归档修复继续保持独立边界；
5. 原`cb2f743`分类说明已校正并与本状态收口文档合并，不再保留单独且不准确的分类提交；
6. 改写后`main`预计比Synology领先14个提交、比GitHub领先9个提交，均只允许后续快进推送。

## 任务状态收口

- 修正`REQ-008`、`REQ-009`和`REQ-010`的过期状态；
- 8张已完成任务卡已从活动目录移动到`docs/tasks/archive/2026-08/`，并从活动索引移除；
- “TEST顺序镜像构建”“TEST数据库初始化脚本修复”和“Windows Git归档LF修复”的剩余门禁统一由`0.1.2-test`重建与数据库恢复任务承接；
- 角色工作台两张规范卡完成后归档，基础实现继续等待V005迁移、服务器回读和12角色浏览器验收；
- 全业务页面配置规范卡完成后归档，后续按页面登记、配置版本、组件权限、表格文案和逐页迁移拆分实现任务。

## 禁止范围

- 不执行Git推送，不创建发布标签；
- 不以`-Apply`运行`backup_sync.ps1`；
- 不连接229或ECS 2，不构建、不执行SQL、不部署；
- 不读取、输出或提交真实环境文件和任何秘密。

## 验收

- [x] Synology和GitHub `main`均完成远端只读核验；
- [x] 改写前完整Git bundle已创建并校验；
- [x] 混合提交拆分为SQL迁移和Git同步安全门禁两个原子提交；
- [x] 原分类文档的类别和提交引用已校正；
- [x] 过期需求状态和任务头部状态已按现有证据修正；
- [x] `TASKS.md`活动任务链接完整、无重复且无未登记活动卡；
- [x] 改写后提交拓扑、文件边界、PowerShell 7/5.1、Bash、JSON、V005唯一登记和差异检查：`PASS`；
- [x] 8张已完成任务卡移动到`docs/tasks/archive/2026-08/`，活动索引和归档链接核对通过；
- [x] Git推送、服务器、构建、SQL和部署：`NOT_RUN`。
