# 2026-08-01 角色工作台可视化配置规范

## 状态

- 需求：`REQ-013`
- 当前状态：`VERIFIED / COMMITTED / ARCHIVED（代码实现与浏览器验收由后续任务承接）`
- Git提交：已完成（`f04c46b`）
- 数据库执行：未授权
- 部署：未授权

## 目标

将用户确认的“全部业务角色界面由管理员受控配置”固化为可直接指导后续开发和验收的权威规范，明确角色、页面、组件、按钮、表格列、主题、系统骨架、发布版本及权限边界。

## 范围

- 涉及模块：产品设计、角色权限、管理端前端、主题与工作台、DYZ 功能覆盖和验收；
- 预计文件：需求登记、产品规范、角色数据范围、前端架构、主题规范、DYZ 覆盖矩阵、验收矩阵和上线清单；
- 数据/API/配置：本任务只定义配置作用域、稳定标识、发布流程和验收规则，不新增运行时数据结构或接口。

## 禁止范围

- 不修改 `backend/`、`frontend/`、`portal/` 或 `deploy/`；
- 不复制 DYZ 数字主键、配置数据、生产参数或运行代码；
- 不通过界面配置授予权限、改变数据范围、绕过状态机或执行任意 CSS、JavaScript、SQL；
- 不构建、不执行 SQL、不连接服务器、不提交或推送 Git。

## 验收

- [x] 全部业务角色及公共门户均有明确的可配置界面范围；
- [x] 主题、系统骨架、页面、类别、组件、表格列、按钮和状态文案的作用域与唯一来源明确；
- [x] 配置显示与后端权限、数据范围和业务状态的边界明确；
- [x] 草稿、预览、校验、发布、回滚和 DYZ 映射具有验收用例；
- [x] 文档链接、REQ 状态和任务范围一致。

## 实际变更与证据

### 修改文件及原因

- `TASKS.md`、`docs/requirements/CREHN_REQUIREMENTS.md`：登记任务和 `REQ-013`，保持需求、任务和权威规范可追踪；
- `CREHN_PRODUCT_RULE.md`：确立管理员受控配置、继承层级及界面显示不能替代权限的产品不变量；
- `CREHN_ROLE_DATA_SCOPE.md`：划分超级管理员、活动管理员、次活动管理员、普通角色和监督审计员的配置权限；
- `CREHN_FRONTEND_ARCH_V1.0.md`：补齐 12 个业务角色，定义统一配置中心、表格列设计器、角色系统骨架、草稿发布和 DYZ 映射；
- `CREHN_THEME_FILE_SYSTEM.md`：定义类型化主题令牌、品牌区、三种导航骨架、用户偏好和门户/工作台隔离；
- `CREHN_DYZ_FUNCTION_COVERAGE.md`：记录 DYZ 菜单外观、导航、品牌、看板和表格列的重构处理方式；
- `CREHN_ACCEPTANCE_MATRIX.md`、`CREHN_FINAL_CHECKLIST.md`：增加配置权限、表格列、按钮、角色骨架、发布回滚、用户偏好、门户隔离和 DYZ 映射验收项。

### 依赖、配置、接口和数据库

- 新增第三方依赖：否；
- 新增运行时配置或环境变量：否；
- 新增或修改 API：否；
- 数据库变更或 SQL：否。

### 已执行检查

- `git diff --check`：`PASS`；
- 相关 Markdown 本地链接检查：`PASS`；
- 前端角色工作台矩阵检查：`PASS（12/12）`；
- 验收 Case ID 重复检查：`PASS`。

### 未处理与未验证

- 管理端、后端、数据库和门户代码：`NOT_RUN`，本任务未授权修改；
- 登录态角色预览、配置保存回读、发布和回滚：`NEEDS_BROWSER / NEEDS_SERVER`；
- DYZ 配置包真实映射：`NEEDS_SERVER`，后续必须使用固定脱敏样例包验证；
- Git提交、推送、数据库执行和部署：均未授权、未执行。
