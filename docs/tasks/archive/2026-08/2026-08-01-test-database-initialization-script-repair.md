# 2026-08-01 TEST数据库初始化脚本修复

## 状态

- 需求：`REQ-007`、`REQ-010`
- 当前状态：`ARCHIVED / VERIFIED_LOCAL / SUPERSEDED_BY_CURRENT_TEST_DATABASE_TASK`
- 用户已授权：仅修复TEST数据库初始化脚本的MariaDB就绪检查和初始化文件循环。

## 目标

修复229首次初始化实测发现的两个问题：MariaDB认证尚未可用时被过早判定为就绪，以及初始化循环只执行首个文件却写入完成标记。

## 实测依据

- 第一次初始化在容器刚启动后通过 `mariadb-admin ping`，随后认证查询返回 `ERROR 1045`；稍后只读回读确认容器环境密码可正常登录且目标库表数为0。
- 第二次初始化仅输出 `database_apply=001_framework.sql` 后即写入完成标记。
- 镜像实际包含44个 `/opt/crehn/init` 文件；回读只有25张基础表，`sys_user` 存在，但 `activity`、`participant_profile`、`flow_definition` 不存在。

## 修改范围

- `deploy/linux/crehn-deploy.sh`
  - 使用带目标数据库和root认证的 `select 1` 查询判断MariaDB真正就绪；
  - 在执行任何容器命令前，将排序后的初始化文件路径完整载入Bash数组；
  - SQL和Shell初始化命令不继承文件枚举输入；
  - 完成状态输出实际遍历的初始化文件数量。
- `TASKS.md`及本任务卡：记录授权边界、问题证据和验证结果。

## 禁止范围

- 不连接服务器，不执行SQL，不删除或重建当前部分初始化数据库；
- 不删除或修改远端错误完成标记；
- 不暂存资产、不构建镜像、不启动、停止或修改容器；
- 不提交或推送Git，不修改真实环境文件或秘密。

## 验收

- [x] 就绪检查必须通过真实认证查询，不能仅依赖服务进程存活；
- [x] 初始化文件列表不受循环体标准输入消费影响；
- [x] 空文件清单必须阻断，完整执行后输出文件数量；
- [x] Bash语法检查、静态回归断言和 `git diff --check` 通过；
- [x] 服务器、SQL、数据清理、构建、Git提交和推送均为 `NOT_RUN`。

## 本地验证

- Git Bash `bash -n deploy/linux/crehn-deploy.sh`：`PASS`；
- 独立循环回归模拟：在循环体主动消费标准输入的条件下，数组方案仍完整处理3/3个模拟初始化文件：`PASS`；
- 静态断言：认证 `select 1`、旧 `mariadb-admin ping` 移除、`mapfile`数组、数组遍历、空清单阻断、SQL生产端和Shell脚本标准输入隔离、完成文件数量输出：全部 `PASS`；
- `git diff --check`：`PASS`；
- 修改差异私钥头和AWS样式访问密钥扫描：`PASS`；
- 真实数据库恢复和重新初始化：`NOT_RUN`，需要新的数据清理、构建、SQL和服务器授权。

## 风险与后续门禁

当前229数据库只有基础框架表且存在错误完成标记。代码修复不会自动修复远端状态；后续需要先生成新的不可变版本，再对当前部分初始化数据制定清理或恢复方案，并分别获取构建、数据删除、SQL和部署授权。
