# 2026-07-31 TEST真实环境文件生成

## 状态

- 需求：`REQ-007`、`REQ-010`
- 当前状态：`VERIFIED / SECRET_PRESERVED / ARCHIVED`
- 用户已授权：本机临时安装最小 BCrypt 工具，并通过隐藏输入生成真实 `deploy/env/test.env`
- 真实环境文件保持Git忽略且不得提交；新的推送、构建、容器、SQL和生产操作未授权

## 目标

在不泄露密码或其他秘密、不向 Git 写入秘密文件的前提下，生成 TEST 独立运行环境文件，为后续只读预检和已单独授权的构建流程提供配置输入。

## 范围

- 临时安装不写入项目依赖的 BCrypt 生成工具；
- 以本机交互隐藏输入获取首个管理员密码，并仅写入 BCrypt 哈希；
- 使用独立随机值生成数据库、Redis、MinIO、JWT和监控秘密；
- 使用 `cunchu` 作为 TEST MinIO 根用户名，绑定当前完整 Git 修订；
- 仅校验字段完整性、占位符、密码哈希格式和 Git 忽略状态。

## 禁止范围

- 不输出、提交、推送或上传任何秘密；
- 不修改 `deploy/.env`、Compose、业务代码、数据库或迁移；
- 不连接服务器，不暂存资产，不构建或启动容器，不执行 SQL。

## 验收

- [x] 真实 `deploy/env/test.env` 已生成且未被 Git 跟踪；
- [x] 所有必填秘密均非模板占位符；
- [x] 首个管理员密码为有效 BCrypt 哈希；
- [x] 运行边界、项目名、端口、保护项目和完整修订值通过静态校验；
- [x] 临时工具和秘密临时文件均已清理。

## 验证

- BCrypt 通过本机隐藏输入生成，成本为 `12`；明文密码未写入文件、命令行或任务卡；
- 运行环境文件仅位于被 Git 忽略的 `deploy/env/test.env`，本机 ACL 已限制为当前 Windows 用户；
- 已校验必填字段、非占位符、`test`/`crehn-test`/`/srv/crehn-test` 边界、MinIO 根用户名 `cunchu`、完整 Git 修订和 BCrypt 格式；
- 本机临时 BCrypt 工具、哈希临时文件和状态文件均已清理；
- 服务器连接、资产暂存、构建、容器、SQL、Git 提交和推送：`NOT_RUN`。
