# REQ-013A 业务页面表头与表格配置运行时接入

## 状态

- 需求：`REQ-013A`
- 当前状态：`ARCHIVED / VERIFIED_THROUGH_0.1.9 / BROWSER_ADMIN_VERIFIED`
- 稳定页面键：`reviewAssignment`、`scoreSummary`、`signedSheets`
- 数据库执行：未执行
- 构建与部署：未执行（当前不可变修订仍为 `345c3c86…`，暂存会遗漏本轮未提交代码）
- Git提交：本轮已授权；推送：未授权、不执行

## 目标

将评审分配、评分汇总、签名表汇总三个已登记页面接入现有表头/表格配置模型，允许在白名单内配置显示列和表头文案，同时保持评分、签名、审计字段、操作权限和服务端数据范围不可被配置改写。

## 范围

- 扩展前后端表格页面稳定键和默认配置；
- 为三个页面提供受保护的内置列清单和操作语义；
- 复用现有表格配置读写接口和归一化逻辑；
- 无权限的组件或操作显示“请先在角色权限管理中增加权限”，不得自动授权；
- 增加针对稳定键、默认列和权限提示的本地测试。

## 禁止范围

- 不修改评分、签名、审计、状态机和数据范围规则；
- 不执行 SQL，不保存 TEST 业务配置，不连接服务器；
- 不新增第三方依赖，不复制 DYZ 数据或数字 ID；
- 不提交或推送 Git。

## 验收

- [x] 三个稳定键可由配置模型安全归一化；
- [x] 三个页面默认列和必要保护列可回读；
- [x] 工作台配置入口缺少权限时显示“请先在角色权限管理中增加权限”，没有自动授权；
- [x] 前端 TypeScript 检查通过；Vitest 全量 `12` 个测试文件、`83` 个测试通过；
- [ ] 后端相关测试、TEST 构建部署和角色浏览器验收另行执行。

## 实际变更与证据

- 扩展前后端 `ArtListTablePageKey` / `LIST_TABLE_PAGE_KEYS`：`reviewAssignment`、`scoreSummary`、`signedSheets`；
- 增加三个页面的默认列、白名单列键、操作槽位和归一化保护；`actions`、状态及签名/评分审计字段不会因配置被删除；
- 评审分配主表、评分汇总主表、签名表汇总主表读取统一配置的列显隐、表头和宽度；业务 API、状态机、数据范围和已有页面专用配置保持不变；
- 工作台配置导航在缺少权限时保留可见入口并提示，不修改权限集合；
- `git diff --check` 通过；未执行 SQL、服务器连接、构建或部署；本轮只提交本功能相关文件，不推送。
- E 批次门禁：TEST 暂存入口按 `CREHN_SOURCE_REVISION` 生成不可变归档；提交后需同步更新 TEST 真实环境修订，再暂存、构建和部署。

## 授权状态

- 代码修改：已授权；
- 数据库、服务器、构建、部署：尚未执行；
- Git提交：已授权；推送：未授权、未执行。
