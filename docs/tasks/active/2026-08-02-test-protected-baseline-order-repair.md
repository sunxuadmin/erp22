# 2026-08-02 TEST项目A保护基线顺序误报修复

## 状态

- 需求：`REQ-010`
- 当前状态：`IMPLEMENTING / COMMIT_NOT_AUTHORIZED`
- 部署脚本修改：已获授权
- Git提交、重新构建、资产暂存和TEST部署：已获授权
- 数据库初始化：禁止
- Git推送：禁止

## 目标

修复TEST部署对项目A保护基线的顺序敏感误报，使基线比较关注容器身份、镜像、状态、健康、重启、OOM和端口绑定等稳定安全字段。

## 范围

- 修改：`deploy/linux/crehn-deploy.sh`的`inspect_project_containers`标准输出；
- 新建：本任务卡并更新`TASKS.md`活动索引；
- 后续：重新生成`0.1.5-test`不可变归档并部署。

## 禁止范围

- 不修改项目A容器、网络、数据或配置；
- 不执行数据库初始化、迁移或SQL；
- 不删除既有TEST备份或不可变归档；
- 不连接ECS 2；
- 不推送Git或发布标签。

## 验收

- [ ] Bash语法和静态差异检查通过；
- [ ] 同一项目A容器的挂载/网络JSON顺序变化不再导致基线误报；
- [ ] 0.1.5-test镜像和归档修订一致；
- [ ] 完整TEST服务健康，项目A五容器基线稳定且健康；
- [ ] 管理端浏览器验收可继续进行。

## 实际变更与证据

待实施后回填文件、原因、验证结果、未处理风险和授权状态。
