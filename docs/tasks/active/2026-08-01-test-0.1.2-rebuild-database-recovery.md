# 2026-08-01 TEST 0.1.2重建与数据库恢复

## 状态

- 需求：`REQ-007`、`REQ-010`、`REQ-013`
- 当前状态：`CONFIRMED_PLAN / HISTORY_RECONCILIATION_PENDING / BUILD_NOT_AUTHORIZED / SERVER_NOT_AUTHORIZED / SQL_NOT_AUTHORIZED / DEPLOY_NOT_AUTHORIZED`
- 文档规划：已授权
- 本次规划文档本地提交：已授权；后续代码提交和Git推送：未授权

## 目标

以完成Git边界收口后的明确提交构建新的不可变`0.1.2-test`产物，验证Windows归档保持Shell脚本LF，再在完整备份和可恢复隔离的前提下替换仅`crehn-test`的部分初始化数据库，执行全部初始化入口并完成数据库只读回读。数据库成功前不部署Backend、Web、Redis、MinIO或其他服务。

## 前置事实

- `0.1.1-test`数据库初始化在`002_bootstrap_admin.sh`因CRLF失败；当前已知只有25张框架表、`sys_user=0`且无完成标记；
- 已校验备份`20260801T092450Z-0.1.0-test-pre-reinitialize`必须保留，不得永久删除；
- `e9de0e8`已在唯一Windows源码归档命令固定`core.autocrlf=false`，但尚未通过229上的新版本构建验证；
- 当前数据库镜像定义包含45个`/opt/crehn/init`入口，包括管理员引导、TEST对象存储数据库配置和V005角色工作台稳定键迁移；
- 项目A为`dyz-current-shadow`，任何阶段都必须采集并比较其5个容器基线。

## 阶段A：提交与资产前置核对

1. 完成当前未推送提交的职责边界审查，确定唯一构建修订；
2. 只读刷新Synology和GitHub远端，确认没有分叉；
3. 核对真实TEST环境文件仍受Git忽略，只读取所需非秘密状态，不输出密码或哈希；
4. 重新执行TEST只读预检并记录项目A基线、4 GiB Swap、磁盘和端口状态。

## 阶段B：0.1.2-test不可变构建

需要新的服务器连接、资产暂存和构建授权后才可执行：

1. 从明确Git提交以LF安全归档方式暂存TEST源码；
2. 使用真实TEST环境中的管理员Node 4096 MiB堆覆盖，顺序构建`db → backend → web`；
3. 不启动任何新CREHN服务；
4. 验证三个镜像标签和镜像ID、发布清单、归档SHA-256、源码修订和`SUCCESS`标记；
5. 从远端暂存源和数据库镜像内回读两个引导脚本，确认均为`LF-only`且与Git对象一致；
6. 构建后确认项目A五个容器ID、健康、重启和OOM状态符合基线。

## 阶段C：数据库恢复与首次初始化

需要新的服务器写操作、数据隔离和SQL授权后才可执行：

1. 只读复核现有部分数据库、错误标记和历史备份；
2. 为当前`0.1.1-test`部分数据库创建新的逻辑及物理备份并校验；
3. 停止并重建仅`crehn-test`数据库容器，将旧MariaDB目录和错误标记移动到项目专属隔离位置，不永久删除；
4. 使用`crehn-db:0.1.2-test`执行首次初始化，必须完整处理45个初始化入口；
5. 回读完成标记、文件计数、表数量、管理员账号和关键业务表；
6. 验证`sys_workbench_layout.role_key`、V005索引、结果管理员和监督审计员；
7. 确认项目A数据库、容器、网络、卷和入口未发生变化。

## 阶段D：后续完整TEST部署

本任务数据库阶段成功不自动授权部署。后续需另行授权：

- 使用同一`0.1.2-test`发布清单启动Backend、Web、Redis、MinIO和MinIO初始化；
- 执行正式`VerifyLocal`、工具链验证、文件上传下载和登录态浏览器验收；
- 验证12角色工作台、稳定`role_key`保存回读、刷新重登和越权阻断；
- 真实验收通过后再评估`vX.Y.Z`发布标签和ECS 2生产准备。

## 禁止范围

- 不原地修补或复用`0.1.1-test`镜像；
- 不永久删除旧数据库、备份或错误证据；
- 不启动、停止或重建项目A；
- 不连接ECS 1或ECS 2，不执行生产操作；
- 不从ECS 2源码构建；
- 不把真实环境文件、密码、密钥或哈希输出到日志或Git。

## 验收

- [ ] 构建修订已固定且与发布清单一致；
- [ ] 三个`0.1.2-test`镜像、不可变归档、SHA-256和`SUCCESS`完整；
- [ ] 两个数据库Shell引导脚本在远端源码和镜像内均为LF且匹配Git对象；
- [ ] 当前部分数据库具有新增且校验通过的可恢复备份；
- [ ] 45个初始化入口完整执行且完成标记正确；
- [ ] 管理员、关键业务表、V005字段/索引/角色回读通过；
- [ ] 项目A部署前后基线一致；
- [ ] Backend、Web、Redis、MinIO和完整TEST部署：`NOT_RUN`，不在数据库恢复授权中；
- [ ] Git推送、发布标签和生产操作：`NOT_RUN`。
