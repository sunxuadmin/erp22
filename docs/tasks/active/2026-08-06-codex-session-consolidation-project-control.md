# 2026-08-06 Codex 会话整理与项目总控基线

## 状态

- 当前状态：`IMPLEMENTED / DOC_VERIFIED`
- 业务代码：不修改
- Git提交/推送：未授权
- 数据库执行：未授权
- 服务器连接/部署：未授权
- 文件或会话删除：未授权

## 目标

基于当前仓库、Git 状态、当前会话及可见的近期相关会话摘要，建立可持续维护的项目状态、统一任务板、决策、交接、会话主题索引和归档建议，避免关键事实只存在于聊天记录中。

## 允许修改范围

- `TASKS.md`
- 本任务卡
- `docs/codex/`

## 禁止范围

- 不修改业务代码、数据库结构或接口实现。
- 不执行 SQL、服务器连接、部署、Git提交或推送。
- 不删除、覆盖或清理现有文件、未提交修改或 Codex 会话。
- 不执行 `git reset`、`git checkout` 或 `git clean`。

## 验收

- [x] 建立 `PROJECT_STATUS.md`、`TASK_BOARD.md`、`DECISIONS.md`、`HANDOFF.md`、`SESSION_INDEX.md`、`ARCHIVE_PLAN.md` 和索引 README。
- [x] 记录当前分支、提交历史、工作区未提交修改和验证边界。
- [x] 统一任务状态，去除重复任务并标注冲突/待确认项。
- [x] 提取冻结契约、风险、授权边界和三会话工作机制。
- [x] 仅文档范围产生新增修改，`git diff --check` 通过（仅有既有工作区 LF/CRLF 提示）。

## 验证结果

- 七份 `docs/codex/` 文档均存在。
- 任务板七种状态格式检查通过，所有任务使用唯一主编号。
- `git diff --check` 退出码为 0；未运行业务测试、构建、浏览器、数据库、服务器或部署验证。
- 本轮未修改既有业务文件；开始前已存在的门户/CMS/迁移改动保持未提交状态。

## 历史信息可见范围

已读取当前仓库与 Git 证据、最近 50 个 Codex/ChatGPT 任务索引，以及其中 5 个 CREHN 相关任务的近期摘要或部分回合。未读取到全部历史会话全文；未出现在仓库、当前会话或可见摘要中的结论不得视为已核实。
