# 2026-08-03 DYZ→CREHN选择性功能迁移

## 状态

- 需求：`REQ-001`、`REQ-004`、`REQ-013`、`REQ-014`
- 当前状态：`IMPLEMENTING`
- 文档登记：已完成
- 代码修改：REQ-014 第一批已实施，其他迁移项仍未开始
- 数据库执行：未授权、未执行
- 服务器连接：仅保留既有 TEST 证据；ECS 2 未授权、未连接
- 部署：未授权、未执行
- Git提交/推送：未授权、未执行

## 目标

在保留 CREHN 自有角色、权限、路由、数据库、容器、部署目录、业务数据和对象存储边界的前提下，选择性吸收 DYZ 在 2026-07-30 至 2026-08-03 期间已完成且适用于 CREHN 的修复和功能；所有 DYZ 未提交内容只能作为参考，不能直接迁移为完成版本。

## 当前基线

- CREHN 前端为 `frontend/src/views/crehn`，后端核心模块为 `backend/ruoyi-modules/ruoyi-crehn`，接口前缀为 `/crehn/**`。
- CREHN 显示配置当前为布局 v8/12 栅格；已存在自动空白、评分表签名、已签评分表、评分汇总和导出基础接口。
- CREHN 当前未发现 `ReviewSession`、`reviewSessionId` 或评审会话隔离表模型；评审和结果仍主要按 `activityId/categoryId` 工作。
- CREHN 已存在 `crehn:reviewScoreSummary:export` 前后端静态权限，但菜单、角色授权和数据库回读尚未执行。
- CREHN 页面提示已有通知模板/站内消息基础，但未形成可直接替代 DYZ M122 的页面偏好能力。
- DYZ 当前页帮助悬浮面板处于未提交进行中状态，不纳入已完成迁移。

## 迁移矩阵摘要

| 功能 | DYZ状态 | CREHN状态 | 处理结论 |
| --- | --- | --- | --- |
| 深层路由刷新 | 已提交代码，浏览器未重验 | `/crehn/**` 与 `/admin/` 边界不同 | 重新适配路由、Nginx 和静态资源 |
| 表头响应式/36栅格 | 已提交代码 | v8/12栅格，已有部分自动空白 | 前后端布局模型适配，保留旧配置兼容 |
| 签名预览与提交交互 | 已提交代码 | 评分、签名、撤回接口已有 | 以 CRE 组件和权限为准做局部适配 |
| 空值兼容/组别筛选 | 已提交代码 | 有归一化和 `groupCode` 基础 | 补静态测试并核对动态组别解析 |
| 评审组织/会话隔离 | M120已提交 | 当前缺少 session 模型 | 先做 CRE 只读数据预检和迁移设计 |
| 分配双视图/多类别工作区 | 已提交代码 | 有分配工作区和类别配置，但无 session 维度 | 依赖会话隔离后适配 |
| 汇总配置与导出 | 已提交或部分在工作区 | 已有汇总/导出接口和权限 | 重新核对范围、Sheet和角色授权 |
| 页面提示/账号偏好 | 基础存在，帮助面板未提交 | CRE 仅有通知模板/消息基础 | 单独设计 CRE M122 等价能力 |
| 当前页帮助悬浮面板 | `IN_PROGRESS_UNCOMMITTED` | 未发现对应基础能力 | 暂缓，独立任务和独立验收 |

## 实施阶段

1. `CONFIRMED`：完成 CRE 专属差异核对和任务拆分。
2. 基础代码：路由边界、显示配置空值兼容、工作台布局、签名和提交交互。
3. 评审业务：先设计并实现 CRE 评审会话隔离，再处理分配双视图和多类别工作区。
4. 管理配置：评分汇总、签名汇总、动态列、导出范围和 CRE 权限。
5. 页面提示：先完成 CRE 页面提示基础，再评估帮助悬浮面板。
6. TEST 验收：只在获得单独授权后执行数据库、服务器、部署和浏览器验收。

## 禁止范围

- 不覆盖 DYZ 全目录，不做全仓库文本替换。
- 不复制 DYZ 真实数据库数据、用户、角色、权限 ID、服务器路径、容器名或 MinIO 对象。
- 不把 DYZ 的 `art:` 权限、`/art-review/**` 路由或 `reviewSessionId` 数据直接写入 CREHN。
- 不迁移 DYZ 当前未提交的页面帮助悬浮面板改动。
- 不执行 M120/M121/M122 或其他业务 SQL；数据库操作需单独授权。
- 不连接 ECS 2、不部署、不提交、不推送，除非单独确认。

## 验收门禁

- 静态：定向 Vitest、ESLint、vue-tsc、前端构建、后端目标模块编译、`git diff --check`。
- 浏览器：管理员、学校用户、专家用户、路由刷新、窄屏、评审组织、评分提交、签名表、汇总导出和帮助面板。
- 数据库：迁移前备份、结构/索引/权限回读、业务计数、M120/M121/M122 分开记录；当前 `NOT_RUN`。
- 部署：仅 CREHN TEST，记录容器健康、Release、SHA-256、回滚目录和数据服务是否变化；当前 `NEEDS_SERVER`。

## 第一批实际变更与证据

- 已开始 REQ-014 第一批代码实施；范围限于评分模式基础契约、提交校验、分配入口、专家评分入口和评分表显示。
- 第一批已补充仅评语结果在评分汇总、评分记录、动态评审结果列和默认显示配置中的展示，并强制仅评语模式显示评语输入。
- 并行静态审计：CREHN 生产前端基路径为 `/admin/`，Vue history 使用同一上下文，Nginx `/admin/` 已回退到 `/admin/index.html`；`/admin/crehn/**` 深层刷新链路无需新增兼容分支，直接访问根路径 `/crehn/**` 不属于当前部署契约。
- 后端职责审计：`ArtReviewServiceImpl` 同时承载分配、任务查询和评分管理，私有方法共享度较高；本批不做机械拆分或新增转发 Service，待形成可独立测试的真实边界后再拆。
- 本批显示配置/评分展示审计：`artDetailDisplayConfig.ts` 已有空值、旧配置迁移、默认值和规范化测试；结果汇总页已有数值、等级和评语回退展示。为避免重复防御逻辑和无边界拆分，本批不新增兼容分支或拆文件。
- 追加明确漏点修复：通用评审工作台 `ReviewWorkbenchWidget.vue` 的最近评分原先只显示等级/数值，无法显示仅评语记录；现改用共享 `scoreRecordText`，列名调整为“评分结果”，沿用已有评语回退规则。
- 本批静态验证：`frontend` 定向 Vitest 通过 3 个文件、39 个测试（显示配置 18、评分展示 3、表格配置 18）；`vue-tsc --noEmit` 仍仅命中既有未修改文件 `src/views/system/workbench/index.vue:460` 的 `ArtListTablePageKey` 类型错误；测试产生的 `.eslintrc-auto-import.json` 自动导入基线已恢复；`git diff --check` 通过，仅有换行符提示。
- 未修改 SQL、部署配置或 Git 历史；数据库、服务器、部署和推送仍未授权。
- 具体实现完成后按 `IMPLEMENTED → VERIFIED` 回填文件、测试、浏览器、数据库、部署和授权证据。
