# 2026-08-05 公共门户液态玻璃与 CSS 粒子性能增强

## 状态

- 需求：`REQ-002`、`REQ-013`
- 当前状态：`IMPLEMENTED / VERIFY_PENDING`
- Git提交：未授权
- 数据库执行：未授权
- 服务器连接：未授权
- 部署：未授权

## 目标

在现有创意河南公共门户的受控主题体系中增强液态玻璃、毛玻璃和背景粒子表现，优先使用 CSS 合成动画，并在移动设备、低性能设备和减少动态效果设置下自动降级。

## 范围

- 增加受控玻璃高光、阴影和粒子令牌；
- 使用现有 CSS 动画粒子层，不引入 Canvas/WebGL 或新运行时依赖；
- 为毛玻璃不支持浏览器保留不透明背景回退；
- 复用现有 performance-lite、prefers-reduced-motion 和用户动效开关；
- 仅修改门户前端、主题配置和本任务文档。

## 禁止范围

- 不开放任意 CSS、JavaScript、外部 HTML、iframe 或可执行表达式；
- 不修改业务权限、数据范围、CMS 发布边界或数据库结构；
- 不执行数据库、服务器、构建、部署、Git提交或推送操作；
- 不修改 DYZ，不复制 DYZ 配置、数据或密钥。

## 验收

- [x] 液态玻璃高光、毛玻璃表面和背景粒子使用 CSS 合成属性；
- [x] 低性能、移动端、减少动态效果和不支持 backdrop-filter 时提供稳定降级路径；
- [x] 主题令牌有范围约束并可由已发布主题覆盖；
- [x] `git diff --check` 通过；主题变量定义和引用静态扫描通过；
- [x] `portal` 类型检查与构建通过，`frontend` 生产构建通过。
- [x] `frontend` 全量类型检查：QA-001 修复后本轮重跑通过。
- [ ] 桌面/平板/手机浏览器验收：`NEEDS_BROWSER`。

## 实际变更

- 新增玻璃高光、阴影、粒子透明度、粒子大小和粒子光晕主题令牌，并加入后端主题白名单和管理端配置项。
- 复用现有背景粒子 DOM，改为 CSS radial-gradient、`transform`、`opacity` 和 `will-change` 合成动画。
- 增加顶部液态玻璃高光扫光；低性能模式降低模糊、阴影、粒子数量，减少动态效果时隐藏粒子。
- 保留 `backdrop-filter` 不支持时的不透明背景回退，不引入 Canvas/WebGL 或新依赖。

## 验证证据

- `portal`: `pnpm build` 通过；Vite 仅提示 `config.js` 未声明 `type="module"`，不影响产物生成。
- `frontend`: `pnpm build:prod` 已有通过证据；本轮全量 `pnpm typecheck` 与16文件/94测试通过。
- 未执行服务器连接、数据库 SQL、部署、浏览器验收或 Git 提交/推送。
