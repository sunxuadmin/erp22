# 2026-08-01 会话分类提交归档

## 说明

本清单记录以`7a2f8ce`为共享基线完成的本地历史收口结果。原混合提交已拆分，原分类说明已校正并并入最终状态文档提交；当前清单不得作为推送授权。

## 后端

- `3246b45 feat(backend): 完善工作台布局角色域兼容与回退处理`
  - 仅包含工作台领域对象和服务端角色稳定键兼容、错误回退处理。

## 前端

- `3f7f546 feat(frontend): 收敛角色工作台配置展示与看板回退体验`
  - 仅包含工作台配置页面、角色映射、通用看板和前端测试。

## 数据库迁移

- `11b6cf8 feat(sql): 完整登记角色工作台稳定键迁移`
  - 统一包含V005迁移、后端SQL清单、数据库镜像入口和部署迁移清单；
  - 未执行SQL、数据库清理或部署。

## Git同步安全门禁

- `8b1b409 feat(git): 增加CREHN远端同步安全门禁`
  - `backup_sync.ps1`默认只输出计划；
  - 实际推送要求显式远端、`-Apply`和精确确认串；
  - 固定推送`main:main`，不再使用`push --all`，标签必须单独开启；
  - 本次未运行实际推送。

## 工作台与产品规范

- `f04c46b docs: 同步角色工作台任务与架构验收文档`
- `cebe101 docs: 完善全业务页面受控配置规范`

## 部署修复

- `4ce245c fix(deploy): 修复TEST数据库初始化就绪与文件遍历`
- `e9de0e8 fix(deploy): 保持Windows Git归档中的Shell脚本为LF`

## 收口结论

- 后端、前端、数据库、Git同步、产品规范和部署修复均保持独立提交边界；
- 原分类说明误把数据库初始化脚本修复列入文档类别的问题已校正；
- 历史重排只影响尚未推送到GitHub的本地尾部提交；
- 推送、服务器连接、构建、SQL和部署仍需分别授权。
